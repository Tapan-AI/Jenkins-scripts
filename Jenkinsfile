pipeline {
    agent any
    stages {
        stage('Run External Script') {
            steps {
                sh './first-script.sh' // Assuming my_script.sh is in the workspace root
            }
        }
    }
}
